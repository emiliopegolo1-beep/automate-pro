{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "pip install -r requirements.txt",
    "watchPatterns": ["react-dist/**", "server.py", "Procfile", "requirements.txt"]
  },
  "deploy": {
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
